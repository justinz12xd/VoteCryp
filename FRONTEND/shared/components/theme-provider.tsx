import * as React from "react";

export function ThemeProvider({ children }: Readonly<{ children: React.ReactNode }>) {
	// minimal stub theme provider; expand as needed.
	return <>{children}</>;
}

export default ThemeProvider;
